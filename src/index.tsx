import React from "react";
import ReactDOM from "react-dom";
import Popupalert from "./components/elements/Popupalert/Popupalert";

ReactDOM.render(
  <React.StrictMode>
    <Popupalert />
  </React.StrictMode>,
  document.getElementById("root")
);
